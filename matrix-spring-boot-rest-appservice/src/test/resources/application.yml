matrix:
  appservice:
    hsToken: validToken
  client:
    homeServer:
      hostname: localhost
      secure: false
    token: superSecretToken

logging:
  level:
    org.springframework.security: TRACE